type Props = {
 
}
export const CheckoutBtn = ({}: Props) => {
    return ( 
        <div>
            <button className="w-full text-sm bg-black text-white p-3">
                Check out
            </button>
        </div>
    );
}